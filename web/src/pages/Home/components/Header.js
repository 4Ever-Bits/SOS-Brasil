import React from "react";
import { Typography } from "@material-ui/core";

export default function Header() {
  return (
    <React.Fragment>
      <Typography style={{ color: "#FFF" }}>Área do Operador</Typography>
    </React.Fragment>
  );
}
